# Agents Guide (Choukai)

- Tests: `npm test --workspace=Choukai`
- Build: `npm run build --workspace=Choukai`
- Lint/format: `npm run lint --workspace=Choukai`
- Focus: map/world/position logic in `src/core`; keep coordinate math deterministic for tests.
